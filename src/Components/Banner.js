import React from 'react';
import "../styles/banner.scss";

export const Banner = () => {
    return(
        <div>
            <h2 id={"banner"}>BANNER ICS!</h2>
        </div>
    );
}

export default Banner;